services:
  - type: web
    name: familyhub
    runtime: python
    buildCommand: pip install -r requirements.txt && python setup_cloud.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: DATABASE_TYPE
        value: sqlite